<%= form_for(@mfnw, :html => {:multipart => true}) do |f| %>
  <% if @mfnw.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mfnw.errors.count, "error") %> prohibited this mfnw from being saved:</h2>

      <ul>
      <% @mfnw.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :genre %><br />
    <%= f.text_field :genre %>
  </div>
  <div class="field">
    <%= f.label :picture %><br />
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>

  </div>
<% end %>
